postgres-config {
  n-threads = 32
  url = "jdbc:postgresql://postgres-rw.postgres.svc.cluster.local:5432/books_db_scala"
  user = "admin"
  user = ${?DB_USER}
  pass = ${DB_PASS}
}

ember-config {
  host = "0.0.0.0"
  port = 5012
}

include "application.local.conf"
